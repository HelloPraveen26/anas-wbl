'use client';

import FAQSection from '../components/FAQSection'; // adjust the path if needed

const Home = () => {
  return (
    <div>
      {/* Your other sections like Hero, Features, etc. */}
      
      {/* Add the FAQ section here */}
      <FAQSection />
    </div>
  );
};

export default Home;
