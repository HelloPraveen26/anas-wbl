[project]
name = "telephony-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "livekit-agents[openai,turn-detector,silero,cartesia,deepgram,elevenlabs]~=1.2",
    "livekit-plugins-noise-cancellation~=0.2",
    "python-dotenv",
    "fastapi[all]",
    "openai>=2.0",
]
